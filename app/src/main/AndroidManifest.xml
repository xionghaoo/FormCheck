<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xh.formmodules">

    <application
        android:name=".FormApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UserLoginActivity"
            android:theme="@style/LoginPageTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FormHomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTop">

        </activity>
        <activity android:name=".nfcbound.AllMachineActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".nfcbound.NFCBoundActivity"></activity>
        <activity android:name=".LogActivity"/>
    </application>

</manifest>